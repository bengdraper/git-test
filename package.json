{
  "name": "git-test",
  "version": "1.0.0",
  "description": "GIT and NPM",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run lite",
    "lite": "lite-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "save-dev": "^0.0.1-security"
  },
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
